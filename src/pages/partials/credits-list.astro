---
import { db, Credit } from "astro:db";

const credits = await db.select().from(Credit);
---

<ul id="credits-list">
  {
    credits.length > 0 ? (
      credits.map((credit) => <li>credit.id: {credit.id}</li>)
    ) : (
      <p>No credits found</p>
    )
  }
</ul>
