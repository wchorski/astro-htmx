---
import type { FieldOption, FieldSlot, SelectFieldType } from "@ty/FieldConfig";
import type { HTMLAttributes } from "astro/types";

interface Props extends Omit<
  HTMLAttributes<"select">,
  "type" | "value" | "checked" | "name"
> {
  formId?: string;
  name: string;
  label?: string;
  value?: string | number;
  fieldError?: string | null;
  inputAttrs?: Omit<FieldSlot, "label" | "value" | "type">;
  options: FieldOption[] | undefined;
}
const { formId, inputAttrs, name, label, value, fieldError, options } =
  Astro.props;
---

<label>
  <span class="text">{label ?? name}</span>
  <select {...inputAttrs} form={formId} name={name}>
    {
      options?.map((opt) => (
        <option
          value={opt.value}
          {...(value && opt.value === value ? { selected: true } : {})}
        >
          {opt.label}
        </option>
      ))
    }
  </select>
  {fieldError && <span class="field-error">{fieldError}</span>}
</label>
